{
  "buildCommand": "cd traveloop && prisma generate && next build",
  "outputDirectory": "traveloop/.next",
  "installCommand": "cd traveloop && npm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/$1"
    }
  ]
}
